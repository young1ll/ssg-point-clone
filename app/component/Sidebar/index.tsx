const menuList = "";
const Sidebar = ({
  toggle,
  toggleHandler,
}: {
  toggle: boolean;
  toggleHandler: () => void;
}) => {
  //TODO: menuList 분류하기
  const filteredMenuList = "";

  return (
    <>
      {/* TODO: 최상위 div className에 toggle에 따른 변화 반영 */}
      <div>
        <div className=""></div>
      </div>
    </>
  );
};
